#include<stdio.h>
int main()
{
    int k=35;
    printf("\n %d%d%d", k==35,k=50,k>40); // 35==35 so output will be true that means 1 , k=50 so output will be the new assigned value of k ,50 ; k=35>40 so output will be false 0
}